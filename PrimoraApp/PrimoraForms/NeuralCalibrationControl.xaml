<UserControl x:Class="Primora.PrimoraForms.NeuralCalibrationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Image Source="/Primora;component/Resources/neural_ai_gaming_viz.png" Opacity="0.1" Stretch="UniformToFill"/>
        <StackPanel Margin="30">
            <TextBlock Text="Neuro-Kinetic Precision Shield" FontSize="28" FontWeight="Light" Foreground="#FFFFFF" Margin="0,0,0,5"/>
            <TextBlock Text="Next-Gen Signal Fidelity &amp; Adaptive Deadzone Mitigation" Foreground="#80FFFFFF" FontSize="14" Margin="0,0,0,30"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Margin="0,0,10,0" BorderThickness="0.5" BorderBrush="{DynamicResource GlassBorder}" Background="{DynamicResource GlassColor}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Signal Stability Matrix" FontWeight="Light" Foreground="#FFFFFF" Margin="0,0,0,15"/>
                        <ProgressBar x:Name="jitterProgress" Height="4" Value="15" Foreground="#FFFFFF" Background="#10FFFFFF" Margin="0,0,0,20"/>
                        
                        <StackPanel Opacity="0.7">
                            <TextBlock Text="Variance Deviation: 0.002ms" Foreground="#E0E0E0" Margin="0,5"/>
                            <TextBlock Text="Fidelity Integrity: 99.98%" Foreground="#E0E0E0" Margin="0,5"/>
                        </StackPanel>
                        
                        <Button x:Name="applyNeuralBtn" Content="Synchronize Precision" Background="#20FFFFFF" BorderBrush="#40FFFFFF" Margin="0,30,0,0" HorizontalAlignment="Left" Click="ApplyNeuralBtn_Click"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="10,0,0,0" BorderThickness="0.5" BorderBrush="{DynamicResource GlassBorder}" Background="{DynamicResource GlassColor}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Hardware Integrity Audit" FontWeight="Light" Foreground="#FFFFFF" Margin="0,0,0,15"/>
                        <TextBlock Text="Run a deep-level simulation to detect mechanical wear and component fatigue." Foreground="#80FFFFFF" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,20"/>
                        
                        <StackPanel x:Name="healthResultsPanel" Visibility="Collapsed" Margin="0,0,0,15">
                            <TextBlock x:Name="healthScoreText" Text="Overall Health: 100%" Foreground="#FFFFFF" FontSize="16"/>
                            <TextBlock x:Name="recommendationText" Text="No issues detected." Foreground="#80FFFFFF" FontSize="11" TextWrapping="Wrap" Margin="0,5,0,0"/>
                        </StackPanel>

                        <Button x:Name="runAuditBtn" Content="Execute Integrity Test" Background="#20FFFFFF" BorderBrush="#40FFFFFF" HorizontalAlignment="Left" Click="RunAuditBtn_Click"/>
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Text="Neuro-Kinetic mitigation utilizes ultra-low latency sub-pixel mapping to neutralize hardware degradation and ensure pinpoint accuracy." 
                       Opacity="0.4" FontSize="11" TextWrapping="Wrap" Margin="0,40,0,0" HorizontalAlignment="Center" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
