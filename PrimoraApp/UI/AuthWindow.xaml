<Window x:Class="Primora.UI.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Primora Authentication" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border Background="#CC111111" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" Opacity="0.5" Direction="270" ShadowDepth="5"/>
        </Border.Effect>
        <Grid Margin="30">
            <!-- Close Button -->
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Background="Transparent" Foreground="#88FFFFFF" BorderThickness="0" 
                    FontSize="16" Cursor="Hand" Click="CloseButton_Click"/>
            
            <!-- Unauthenticated State -->
            <StackPanel x:Name="UnauthenticatedPanel" VerticalAlignment="Center" Visibility="Visible">
                <TextBlock Text="PRIMORA NETWORK" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <TextBlock Text="EMAIL ADDRESS" Foreground="#88FFFFFF" FontSize="10" Margin="0,0,0,5"/>
                <TextBox x:Name="EmailField" Background="Transparent" Foreground="White" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1" Margin="0,0,0,20" Padding="5"/>
                
                <TextBlock Text="PASSWORD" Foreground="#88FFFFFF" FontSize="10" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordField" Background="Transparent" Foreground="White" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1" Margin="0,0,0,30" Padding="5"/>
                
                <TextBlock x:Name="StatusMessage" Foreground="#FF5555" FontSize="12" Margin="0,0,0,10" HorizontalAlignment="Center" Visibility="Collapsed"/>
                
                <Button x:Name="SignInButton" Content="SIGN IN" Background="#22FFFFFF" Foreground="White" BorderThickness="1" BorderBrush="#44FFFFFF" Padding="10" Cursor="Hand" Click="SignInButton_Click"/>
                
                <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="12">
                    <Hyperlink Foreground="#88FFFFFF" RequestNavigate="CreateAccount_RequestNavigate" NavigateUri="https://primora-website.vercel.app/#pricing">
                        Create Account
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
            
            <!-- Authenticated State -->
            <StackPanel x:Name="AuthenticatedPanel" VerticalAlignment="Center" Visibility="Collapsed">
                <TextBlock Text="NEURAL LINK ACTIVE" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <TextBlock Text="ACCOUNT" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock x:Name="UserEmailText" Text="user@example.com" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <TextBlock Text="CURRENT TIER" Foreground="#88FFFFFF" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <Border Background="#33FFFFFF" CornerRadius="15" Padding="15,5" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <TextBlock x:Name="UserTierText" Text="PRO" Foreground="White" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                </Border>
                
                <Button x:Name="SyncButton" Content="SYNC PROFILES" Background="#22FFFFFF" Foreground="White" BorderThickness="1" BorderBrush="#44FFFFFF" Padding="10" Margin="0,0,0,10" Cursor="Hand" Click="SyncButton_Click"/>
                <Button x:Name="SignOutButton" Content="SIGN OUT" Background="Transparent" Foreground="#88FFFFFF" BorderThickness="0" Padding="10" Cursor="Hand" Click="SignOutButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
